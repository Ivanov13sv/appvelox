import styled from 'styled-components';

export const Main = styled.main`
	grid-area: main;
`;

export const Title = styled.h2`
	font-style: normal;
	font-weight: 400;
	font-size: 16px;
	line-height: 19px;
	color: #000000;
	height: 46px;
	display: flex;
	align-items: center;
	padding-left: 40px;
`;
